const mainController = require( './main' );
const projectsController = require( './projects' );
const techsController = require( './techs' );

module.exports = {
    mainController,
    projectsController,
    techsController
}